spring:
  application:
    name: pickcar-consumer
  profiles:
    active: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

mq:
  cycle:
    queue: cycle-queue
    exchange: exchange-cycle
    routing-key: cycle.key
  event:
    queue: event-queue
    exchange: exchange-event
    routing-key: event.key
  drive-history:
    queue: drive-history-queue
    exchange: exchange-drive-history
    routing-key: drivehistory.key

logging:
  module-name: consumer
  sensitive-field-rules:
    - path: "none"
      fields: [ "none" ]